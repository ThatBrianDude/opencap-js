{
  "name": "opencap",
  "version": "1.0.1",
  "description": "OpenCap wrapper for interacting with an opencap server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ThatBrianDude",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5"
  }
}
